alguns lembretes e padrões

- todas as variaveis que são de controle, do core, precisa term `$core_` no começo
- todo javascript que é usado pelo core, precisa começar com `core_`
- todo atributo de elemento que é usado pelo core, precisa começar com `data-core-`